image: ruby:2.3 

pages:
  stage: deploy
  script:
  - gem install jekyll
  - gem install redcarpet
  - gem install jekyll-paginate
  - gem install jekyll-sitemap
  - jekyll build -d public/
  artifacts:
    paths:
    - public
  only:
  - master
